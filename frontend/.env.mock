# Loaded automatically by Vite when run with --mode mock (see the dev:mock
# and build:mock scripts in package.json, and mockApi.ts for what this
# switches on). Never used for a real build/dev run.
VITE_MOCK_API=true
