requests
mcp<2.0.0
starlette
uvicorn[standard]
python-multipart
itsdangerous
jinja2
httpx
tzdata
Pillow

# Security floors for transitive / base-image packages flagged by the
# container vulnerability scan. Not imported directly by the app — pinned only
# to keep the built image off the vulnerable versions.
msgpack>=1.2.1        # GHSA-6v7p-g79w-8964 (OOB read on Unpacker reuse), fixed 1.2.1
setuptools>=78.1.1    # CVE-2025-47273 (PackageIndex path traversal), fixed 78.1.1
