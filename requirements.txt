requests
mcp<2.0.0
starlette
uvicorn[standard]
python-multipart
itsdangerous
jinja2
httpx
tzdata
Pillow

# General security floors, not imported directly by the app. NOTE: these do
# NOT fix the container scanner's msgpack/setuptools findings — those come
# from pip's own internal vendored copies (pip/_vendor/vendor.txt), a
# separate, older set of versions from whatever we pin here. That's handled
# by removing pip from the final image in the Dockerfile instead.
msgpack>=1.2.1        # GHSA-6v7p-g79w-8964 (OOB read on Unpacker reuse), fixed 1.2.1
setuptools>=78.1.1    # CVE-2025-47273 (PackageIndex path traversal), fixed 78.1.1
